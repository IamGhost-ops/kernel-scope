global:
  scrape_interval: 5s 


scrape_configs:
  - job_name: 'python-app'
    scheme: https
    metrics_path: /metrics
    static_configs:
      - targets: ['localhost:8443']


    tls_config:
      insecure_skip_verify: true
